<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Name="ContentPage"
    x:Class="SoftwareCW.Views.HierarchyPage">
    <StackLayout>
        <Label Text="{Binding Description}" HorizontalOptions="Center" x:Name="DescriptionLabel" FontSize="Medium" HorizontalTextAlignment="Center" Padding="20, 20, 20, 0"/>
        <ListView ItemsSource="{Binding ChildNodes}" x:Name="HierarchyListView" ItemSelected="HierarchyListView_ItemSelected" SelectedItem="HierarchyListView_Selected" RowHeight="80" VerticalOptions="CenterAndExpand">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextCell Text="{Binding Name}"
                              Detail="{Binding DisplayPriority}" />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button x:Name="evaluateButton" Text="EVALUATE" BackgroundColor="Orange" Margin="10" Clicked="evaluateButton_Clicked"/>
    </StackLayout>
</ContentPage>

